require 'spec_helper'

describe Chessboard do
  it 'has a version number' do
    expect(Chessboard::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
